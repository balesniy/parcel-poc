mixin radio(label, name, checked)
    label.radio
        input.radio__input(
            type='radio'
            name=name
            checked=checked
        )
        span.radio__box
        | #{label}
